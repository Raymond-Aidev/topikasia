[build]
builder = "nixpacks"
buildCommand = "npx prisma generate --config prisma.config.ts && npm run build"

[deploy]
startCommand = "sh start.sh"
healthcheckPath = "/api/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
