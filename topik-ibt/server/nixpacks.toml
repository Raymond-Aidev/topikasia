[phases.setup]
nixPkgs = ["nodejs_20", "openssl"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npx prisma generate --config prisma.config.ts", "npm run build"]

[start]
cmd = "npx prisma db push --config prisma.config.ts --skip-generate && node dist/server.js"
