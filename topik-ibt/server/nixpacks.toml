[phases.setup]
nixPkgs = ["nodejs_20", "openssl"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npx prisma generate --config prisma.config.ts", "npm run build"]

[start]
cmd = "npx prisma migrate deploy --config prisma.config.ts && node dist/server.js"
